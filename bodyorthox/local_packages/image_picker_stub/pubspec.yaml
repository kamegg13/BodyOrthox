name: image_picker
description: "Stub local — image_picker 1.1.2 API compatible. Utilisé pour dart analyze dans le sandbox Claude Code (TLS pub.dev restreint). En production, remplacer par la vraie dépendance image_picker: ^1.1.2."
version: 1.1.2
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  cross_file: ^0.3.5
